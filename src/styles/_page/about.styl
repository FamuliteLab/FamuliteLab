.about
  +pc()
    padding-top vw(120, $vpc)
  +sp()
    padding-top vw(200, $vsp)

.about__blk
  +pc()
    display flex
    justify-content space-between
    align-items center
    margin-top vw(100, $vpc)

.about__img 
  +pc()
    width vw(400, $vpc)
  +sp()
    margin-top vw(80, $vsp)